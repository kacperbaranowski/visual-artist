---
import Layout from "../layouts/Layout.astro";
---

<Layout title="About | Artist Name">
  <section class="min-h-screen pt-32 pb-24 px-8 md:px-16 lg:px-24">
    <div class="max-w-6xl mx-auto">
      <div class="mb-16 md:mb-24">
        <div class="overflow-hidden">
          <p class="about-line text-accent text-sm tracking-[0.3em] uppercase mb-4 translate-y-full">About</p>
        </div>
        <div class="overflow-hidden">
          <h1 class="about-line font-serif text-5xl md:text-7xl lg:text-8xl font-light translate-y-full">First Lastname</h1>
        </div>
      </div>

      <div class="grid grid-cols-1 lg:grid-cols-2 gap-16 lg:gap-24">
        <div class="about-image relative">
          <div class="relative overflow-hidden aspect-[3/4]">
            <div class="about-image-reveal absolute inset-0 bg-bg z-10"></div>
            <img src="https://picsum.photos/seed/portrait/800/1067" alt="Artist portrait" class="w-full h-full object-cover" />
          </div>
          <div class="absolute -bottom-6 -right-6 w-48 h-48 border border-accent/20 -z-10"></div>
        </div>

        <div class="flex flex-col justify-center">
          <div class="space-y-6 text-text-muted leading-relaxed">
            <div class="overflow-hidden"><p class="about-text translate-y-full">Born with an innate curiosity for color and form, I began my artistic journey at an early age, exploring the boundaries between abstraction and emotion.</p></div>
            <div class="overflow-hidden"><p class="about-text translate-y-full">My work has been exhibited internationally across galleries in Europe, Asia, and North America. Each piece represents a dialogue between light, texture, and the human experience.</p></div>
            <div class="overflow-hidden"><p class="about-text translate-y-full">Currently based between studios, I continue to push the boundaries of contemporary painting while mentoring emerging artists through workshops and residencies.</p></div>
          </div>

          <div class="grid grid-cols-3 gap-8 mt-12 pt-12 border-t border-text-muted/20">
            <div class="about-stat opacity-0 translate-y-4"><p class="font-serif text-4xl md:text-5xl text-accent">15+</p><p class="text-text-muted text-sm mt-2">Years</p></div>
            <div class="about-stat opacity-0 translate-y-4"><p class="font-serif text-4xl md:text-5xl text-accent">50+</p><p class="text-text-muted text-sm mt-2">Exhibitions</p></div>
            <div class="about-stat opacity-0 translate-y-4"><p class="font-serif text-4xl md:text-5xl text-accent">200+</p><p class="text-text-muted text-sm mt-2">Artworks</p></div>
          </div>
        </div>
      </div>
    </div>
  </section>
</Layout>

<script>
  import gsap from "gsap";
  import { ScrollTrigger } from "gsap/ScrollTrigger";
  gsap.registerPlugin(ScrollTrigger);

  document.addEventListener("preloaderComplete", init);
  if (!document.getElementById("preloader") || document.getElementById("preloader")?.style.display === "none") init();

  function init() {
    gsap.to(".about-line", { y: 0, duration: 1, stagger: 0.1, ease: "power3.out" });
    gsap.to(".about-image-reveal", { scaleY: 0, transformOrigin: "top", duration: 1.2, ease: "power3.inOut", delay: 0.3 });
    gsap.to(".about-text", { y: 0, duration: 0.8, stagger: 0.1, ease: "power3.out", delay: 0.5 });
    gsap.to(".about-stat", { opacity: 1, y: 0, duration: 0.6, stagger: 0.1, ease: "power3.out", scrollTrigger: { trigger: ".about-stat", start: "top 80%" } });
  }
</script>
